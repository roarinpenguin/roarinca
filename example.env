# Roarin CA Console environment template
#
# Usage:
# 1. Copy this file to `.env` in the project root:
#      cp example.env .env
# 2. Edit the values below to match your environment.
#    - Especially change JWT_SECRET and CA_ADMIN_PASSWORD.
# 3. Do NOT commit `.env` to git (it is ignored via .gitignore).
#
# These variables are consumed by docker-compose and the backend.

# Backend runtime mode
NODE_ENV=production

# Backend network port inside the container (host port is set in docker-compose.yml)
PORT=4000

# Paths inside the backend container for DB and storage
DB_PATH=/data/certui.db
STORAGE_DIR=/data

# Authentication / security
# Long random string used to sign JWTs.
JWT_SECRET=change-me-to-a-long-random-string

# Admin user for the console. Username is fixed to `ca_admin`.
CA_ADMIN_PASSWORD=change-me-to-a-strong-admin-password

# Extra protection flag for CA private key export flows
CA_EXPORT_PROTECTION=enabled

# Frontend API base URL (used by the frontend container build if wired)
VITE_API_BASE=http://backend:4000
